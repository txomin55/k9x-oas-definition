openapi: 3.0.1
info:
  title: K9X API
  description: K9X API Documentation
  license:
    name: Apache 2.0
    url: 'https://springdoc.org'
  version: v0.0.1
externalDocs:
  description: K9X Backend project repo
  url: 'https://github.com/txomin55/k9x-backend'
servers:
  - url: 'http://localhost:4000'
    description: Generated server url
paths:
  /api/dogs:
    get:
      tags:
        - getDogs
      operationId: getDogs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/GetDogListWeb'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
  /login:
    post:
      tags:
        - login
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginWeb'
      responses:
        '200':
          description: OK
          content:
            '*/*':
              schema:
                type: string
  /api/user:
    get:
      tags:
        - getUserData
      operationId: getUserData
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetails'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
  '/api/dogs/{id}':
    get:
      tags:
        - getDog
      operationId: getDog
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetDogWeb'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
  /api/logout:
    get:
      tags:
        - logout
      operationId: logout
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
          content:
            '*/*':
              schema:
                type: string
        '401':
          $ref: '#/components/responses/UnauthorizedError'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    UnauthorizedError:
      description: Authorization header is missing!!
  headers:
    authorizationHeader:
      description: desc
      schema:
        type: string
  schemas:
    GetDogWeb:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        image:
          type: string
    UserDetails:
      type: object
      properties:
        owner:
          type: string
        organizations:
          type: array
          items:
            type: string
    GetDogListWeb:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
          x-faker: name.firstName
        image:
          type: string
    LoginWeb:
      title: LoginWeb
      type: object
      properties:
        idToken:
          type: string
      required:
        - idToken
